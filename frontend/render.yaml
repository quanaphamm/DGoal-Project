services:
  - name: dgoal-frontend
    type: web
    env: static
    staticPublishPath: build
    buildCommand: npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
